//import { render, screen, cleanup } from '@testing-library/react';
//import BookingItem from '../BookingItem;'// to render component we are testing

test('should render BookingItem component', () => {
  expect('true').toBe('true');
})